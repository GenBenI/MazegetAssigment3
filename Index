<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>מצגת: מסע בהערכת מודלי שפה גדולים</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Assistant:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        html {
            scroll-behavior: smooth;
        }
        body {
            font-family: 'Assistant', sans-serif;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }
        .slide {
            transition: transform 0.5s ease-in-out, opacity 0.5s ease-in-out;
            min-height: 80vh; /* Ensure slide takes up most of the viewport height */
            display: none; /* Hide all slides by default */
        }
        .slide-active {
            display: flex; /* Show only the active slide */
            transform: translateX(0);
            opacity: 1;
            position: relative;
        }
        .glassmorphism {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        .code-block {
            background-color: #1E293B;
            color: #E2E8F0;
            padding: 1rem;
            border-radius: 0.5rem;
            font-family: 'Courier New', Courier, monospace;
            direction: ltr;
            text-align: left;
        }
        .code-keyword { color: #93C5FD; }
        .code-string { color: #A5B4FC; }
        .code-comment { color: #64748B; }
        .code-function { color: #818CF8; }
    </style>
</head>
<body class="bg-slate-900 text-white overflow-x-hidden">

    <div id="presentation-container" class="relative w-full max-w-6xl mx-auto py-8 px-4 overflow-hidden">

        <!-- Slide 1: Title -->
        <div id="slide-1" class="slide slide-active flex-col items-center justify-center text-center">
            <div class="glassmorphism p-8 md:p-12 rounded-2xl shadow-2xl">
                <span class="text-indigo-400 font-bold text-lg">פרויקט הערכת מודלי שפה</span>
                <h1 class="text-4xl md:text-6xl font-bold my-4 leading-tight">מסע בהערכת מודלי שפה גדולים</h1>
                <p class="text-xl md:text-2xl text-slate-300 mt-4">מפרויקט מתפתח למערכת הערכה חזקה ואמינה</p>
            </div>
        </div>

        <!-- Slide 2: The Challenge -->
        <div id="slide-2" class="slide flex-col items-center justify-center text-center">
             <div class="w-full">
                <h2 class="text-3xl md:text-5xl font-bold mb-8">האתגר: הערכת איכות אמינה</h2>
                <div class="glassmorphism p-8 rounded-2xl shadow-lg text-right">
                    <p class="text-lg md:text-xl leading-relaxed">
                        המטרה המרכזית הייתה להעריך את איכות הפלט של מודלי שפה גדולים (LLMs) באופן עקבי, מדויק ואובייקטיבי.
                        <br><br>
                        השאלה המנחה: כיצד נוכל לסמוך על כך שהערכות המודל אינן מוטות, מקריות או בלתי יציבות?
                    </p>
                    <div class="mt-8 flex justify-center items-center gap-4 text-indigo-300">
                        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="w-10 h-10"><path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"></path><path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"></path></svg>
                        <span class="text-2xl font-semibold">אמינות</span>
                        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="w-10 h-10"><circle cx="12" cy="12" r="10"></circle><circle cx="12" cy="12" r="6"></circle><circle cx="12" cy="12" r="2"></circle></svg>
                        <span class="text-2xl font-semibold">דיוק</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Slide 3: Starting Point & Pivot -->
        <div id="slide-3" class="slide flex-col items-center justify-center">
            <h2 class="text-3xl md:text-5xl font-bold mb-8 text-center">נקודת התחלה ושינוי כיוון</h2>
            <div class="w-full flex flex-col md:flex-row items-center justify-around gap-8">
                <div class="glassmorphism p-6 rounded-2xl text-center w-full md:w-1/3">
                    <img src="https://placehold.co/100x100/7C3AED/FFFFFF?text=HF" alt="Hugging Face לוגו" class="mx-auto mb-4 rounded-full" onerror="this.onerror=null;this.src='https://placehold.co/100x100/7C3AED/FFFFFF?text=HF';">
                    <h3 class="text-2xl font-bold mb-2">התחלה</h3>
                    <p class="text-slate-300">הפרויקט החל עם מודל של Hugging Face (כנראה Mistral), אך נתקל בקשיים טכניים.</p>
                </div>
                <div class="text-5xl text-indigo-400 transform rotate-90 md:rotate-0">→</div>
                <div class="glassmorphism p-6 rounded-2xl text-center w-full md:w-1/3">
                    <img src="https://placehold.co/100x100/4F46E5/FFFFFF?text=Gemini" alt="Gemini לוגו" class="mx-auto mb-4 rounded-full" onerror="this.onerror=null;this.src='https://placehold.co/100x100/4F46E5/FFFFFF?text=Gemini';">
                    <h3 class="text-2xl font-bold mb-2">שינוי כיוון</h3>
                    <p class="text-slate-300">המעבר למודלי Gemini של גוגל סיפק גמישות ויציבות גבוהות יותר באמצעות API ייעודי.</p>
                </div>
            </div>
        </div>
        
        <!-- Slide 4: Technical Adaptations -->
        <div id="slide-4" class="slide flex-col items-center justify-center">
            <h2 class="text-3xl md:text-5xl font-bold mb-8 text-center">שלב 1: התאמות טכניות</h2>
             <div class="w-full glassmorphism p-8 rounded-2xl shadow-lg">
                <h3 class="text-2xl font-bold mb-4 text-indigo-300">אתגר: עיבוד אצוות (Batch Processing)</h3>
                <p class="mb-6 text-lg">קוד האצוות דרש פלט מובנה (מילון), אך פונקציית ה-API החזירה טקסט פשוט. היה צורך לגשר על הפער מבלי לשנות את הלוגיקה הקיימת.</p>
                <div class="grid md:grid-cols-2 gap-6 items-center">
                    <div class="code-block text-sm">
                        <span class="code-comment"># הפונקציה המקורית</span><br>
                        <span class="code-keyword">def</span> <span class="code-function">call_gemini</span>(prompt):<br>
                        &nbsp;&nbsp;...<br>
                        &nbsp;&nbsp;<span class="code-keyword">return</span> <span class="code-string">"simple text output"</span>
                    </div>
                    <div class="code-block text-sm">
                        <span class="code-comment"># הפתרון: פונקציית "עטיפה"</span><br>
                        <span class="code-keyword">def</span> <span class="code-function">call_gemini_for_batch</span>(prompt):<br>
                        &nbsp;&nbsp;text_result = <span class="code-function">call_gemini</span>(prompt)<br>
                        &nbsp;&nbsp;<span class="code-keyword">return</span> {<span class="code-string">'text'</span>: text_result, <span class="code-string">'runtime'</span>: ...}
                    </div>
                </div>
                <p class="mt-6 text-center text-lg font-semibold text-green-400">התוצאה: תאימות מלאה תוך שמירה על קוד נקי.</p>
            </div>
        </div>

        <!-- Slide 5: Decoding the "Gibberish" -->
        <div id="slide-5" class="slide flex-col items-center justify-center">
            <h2 class="text-3xl md:text-5xl font-bold mb-8 text-center">שלב 2: פענוח ה"ג'יבריש"</h2>
            <div class="w-full flex flex-col md:flex-row items-center justify-center gap-8 p-6 glassmorphism rounded-2xl">
                <div class="text-center w-full md:w-2/5">
                    <h3 class="text-2xl font-bold mb-2">הפלט הראשוני</h3>
                    <div class="code-block text-xs break-all">
                        {'Fluency': {'explanation': '...', 'verdict': 'good'}, 'Grammar': ...}
                    </div>
                     <p class="mt-4 text-slate-300">הפלט נראה כטקסט לא קריא, אך זו הייתה תובנה חשובה.</p>
                </div>
                <div class="text-5xl text-indigo-400 transform rotate-90 md:rotate-0">→</div>
                <div class="text-center w-full md:w-2/5">
                    <h3 class="text-2xl font-bold mb-2">ההבנה: פלט מובנה</h3>
                    <div class="bg-slate-800 p-4 rounded-lg border border-indigo-500">
                        <p class="text-lg text-yellow-300">JSON</p>
                        <p class="text-slate-200 mt-2">הפלט נועד לעיבוד אוטומטי על ידי "מודל שופט" (Judge Model) לצורך מתן ציון כמותי.</p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Slide 6: Refining the Judge -->
        <div id="slide-6" class="slide flex-col items-center justify-center">
            <h2 class="text-3xl md:text-5xl font-bold mb-8 text-center">שלב 3: שכלול מודל השופט (JLM)</h2>
            <div class="w-full grid md:grid-cols-3 gap-6 text-center">
                <div class="glassmorphism p-6 rounded-2xl">
                    <h3 class="text-xl font-bold text-indigo-300 mb-3">שיפור הפרומפט</h3>
                    <p>הנחיה מפורשת לקבלת אובייקט JSON יחיד ותקין, ועידוד מתודולוגיית "הנמקה לפני החלטה".</p>
                </div>
                <div class="glassmorphism p-6 rounded-2xl">
                    <h3 class="text-xl font-bold text-indigo-300 mb-3">תיקון קוד</h3>
                    <p>תיקון שגיאות תחביר בקריאה לפונקציית ה-API וחילוץ נכון של טקסט הפלט.</p>
                </div>
                <div class="glassmorphism p-6 rounded-2xl">
                    <h3 class="text-xl font-bold text-indigo-300 mb-3">ניקוי פלט</h3>
                    <p>הוספת שלב להסרת תווי "עטיפה" לא רצויים מה-JSON כדי להבטיח פענוח תקין.</p>
                </div>
            </div>
        </div>

        <!-- Slide 7: The Breakthrough -->
        <div id="slide-7" class="slide flex-col items-center justify-center text-center">
            <h2 class="text-3xl md:text-5xl font-bold mb-2">פריצת הדרך: הצבעת רוב</h2>
            <p class="text-xl text-slate-300 mb-8">(Majority Vote)</p>
            <div class="glassmorphism p-8 rounded-2xl shadow-lg">
                <p class="text-lg md:text-xl leading-relaxed">
                    כדי להפחית את הסיכון להטיה או טעות של מודל בודד, הפרויקט עבר למערכת רב-מודלית.
                    <br>
                    גישה זו מבטיחה שההחלטה הסופית יציבה, אמינה יותר, ואינה תלויה ב"גחמה" של שופט יחיד.
                </p>
            </div>
        </div>

        <!-- Slide 8: How it Works -->
        <div id="slide-8" class="slide flex-col items-center justify-center">
            <h2 class="text-3xl md:text-5xl font-bold mb-8 text-center">איך זה עובד?</h2>
            <div class="w-full glassmorphism p-8 rounded-2xl">
                <div class="flex flex-col items-center">
                    <!-- Input -->
                    <div class="bg-slate-800 p-4 rounded-lg mb-6 text-center">
                        <p class="font-bold">תיאור מוצר להערכה</p>
                    </div>
                    <svg class="w-8 h-8 text-indigo-400 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path></svg>
                    <!-- Models -->
                    <div class="w-full grid grid-cols-1 md:grid-cols-3 gap-6 text-center mb-6">
                        <div class="bg-indigo-900/50 p-4 rounded-lg">
                            <h4 class="font-bold">Gemini 1.5 Pro</h4>
                            <p class="text-green-400 text-2xl font-bold mt-2">SAFE</p>
                        </div>
                        <div class="bg-indigo-900/50 p-4 rounded-lg">
                            <h4 class="font-bold">Gemini 1.5 Flash</h4>
                            <p class="text-red-400 text-2xl font-bold mt-2">NOT SAFE</p>
                        </div>
                        <div class="bg-indigo-900/50 p-4 rounded-lg">
                            <h4 class="font-bold">Gemini 1.0 Pro</h4>
                            <p class="text-green-400 text-2xl font-bold mt-2">SAFE</p>
                        </div>
                    </div>
                    <svg class="w-8 h-8 text-indigo-400 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path></svg>
                    <!-- Final Decision -->
                    <div class="bg-green-500/20 border border-green-500 p-4 rounded-lg text-center">
                        <p class="font-bold">החלטת הרוב הסופית</p>
                        <p class="text-green-300 text-3xl font-extrabold mt-2">SAFE</p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Slide 9: Automation & Comparison -->
        <div id="slide-9" class="slide flex-col items-center justify-center">
            <h2 class="text-3xl md:text-5xl font-bold mb-8 text-center">שלב 4: אוטומציה והשוואה</h2>
            <div class="w-full glassmorphism p-8 rounded-2xl grid md:grid-cols-2 gap-8 items-center">
                <div>
                    <h3 class="text-2xl font-bold text-indigo-300 mb-4">תהליך אוטומטי</h3>
                    <p class="text-lg">
                        נוצרה פונקציה (`judge_dataframe_mv`) המיישמת את לוגיקת הצבעת הרוב על מערך נתונים שלם.
                        <br><br>
                        התהליך איפשר השוואה ישירה בין התפלגות התוויות של שיטת השופט היחיד לבין שיטת הצבעת הרוב.
                    </p>
                </div>
                <div>
                    <h4 class="text-xl font-semibold text-center mb-4">התפלגות תוויות (דוגמה)</h4>
                    <div class="space-y-4">
                        <div>
                            <p class="mb-1">שופט יחיד</p>
                            <div class="w-full bg-slate-700 rounded-full h-6">
                                <div class="bg-blue-500 h-6 rounded-full" style="width: 65%"></div>
                            </div>
                        </div>
                        <div>
                            <p class="mb-1">הצבעת רוב</p>
                            <div class="w-full bg-slate-700 rounded-full h-6">
                                <div class="bg-green-500 h-6 rounded-full" style="width: 80%"></div>
                            </div>
                        </div>
                    </div>
                     <p class="mt-4 text-center text-sm text-slate-400">הצבעת הרוב הובילה להחלטות יציבות יותר.</p>
                </div>
            </div>
        </div>

        <!-- Slide 10: Takeaways -->
        <div id="slide-10" class="slide flex-col items-center justify-center">
            <h2 class="text-3xl md:text-5xl font-bold mb-8 text-center">מסקנות והצעדים הבאים</h2>
            <div class="w-full grid md:grid-cols-2 gap-8">
                <div class="glassmorphism p-6 rounded-2xl">
                    <h3 class="text-2xl font-bold text-green-400 mb-3">יתרונות מרכזיים</h3>
                    <ul class="list-disc list-inside space-y-2 text-lg">
                        <li><span class="font-semibold">אמינות גבוהה יותר:</span> הפחתת השפעת טעויות של מודל בודד.</li>
                        <li><span class="font-semibold">יציבות:</span> תוצאות עקביות יותר לאורך זמן ועל פני נתונים שונים.</li>
                        <li><span class="font-semibold">חוסן:</span> המערכת פחות פגיעה להטיות ספציפיות של מודל מסוים.</li>
                    </ul>
                </div>
                <div class="glassmorphism p-6 rounded-2xl">
                    <h3 class="text-2xl font-bold text-yellow-400 mb-3">פשרות ושיקולים</h3>
                     <ul class="list-disc list-inside space-y-2 text-lg">
                        <li><span class="font-semibold">זמן ריצה:</span> תהליך ההערכה ארוך יותר באופן טבעי.</li>
                        <li><span class="font-semibold">עלות:</span> שימוש במספר מודלים כרוך בעלות API גבוהה יותר.</li>
                        <li><span class="font-semibold">הצעד הבא:</span> ניתוח כמותי של זמן הריצה והעלות כדי להבין את ה-Trade-offs.</li>
                    </ul>
                </div>
            </div>
        </div>
        
        <!-- Slide 11: Q&A -->
        <div id="slide-11" class="slide flex-col items-center justify-center text-center">
            <div class="glassmorphism p-12 rounded-2xl shadow-2xl">
                <h2 class="text-5xl md:text-7xl font-bold">תודה רבה!</h2>
                <p class="text-2xl md:text-3xl text-slate-300 mt-4">שאלות?</p>
            </div>
        </div>

    </div>

    <!-- Navigation -->
    <div class="fixed bottom-0 left-0 right-0 p-4 bg-slate-900/50 backdrop-blur-sm">
        <div class="max-w-6xl mx-auto flex justify-between items-center">
            <button id="prev-btn" class="bg-indigo-600 hover:bg-indigo-500 text-white font-bold py-2 px-4 rounded-lg transition-all disabled:opacity-50 disabled:cursor-not-allowed">
                → הקודם
            </button>
            <div id="slide-counter" class="text-slate-300">1 / 11</div>
            <button id="next-btn" class="bg-indigo-600 hover:bg-indigo-500 text-white font-bold py-2 px-4 rounded-lg transition-all disabled:opacity-50 disabled:cursor-not-allowed">
                הבא ←
            </button>
        </div>
    </div>

    <script>
        // Wait for the DOM to be fully loaded before running the script
        document.addEventListener('DOMContentLoaded', () => {
            const slides = document.querySelectorAll('.slide');
            const nextBtn = document.getElementById('next-btn');
            const prevBtn = document.getElementById('prev-btn');
            const slideCounter = document.getElementById('slide-counter');
            let currentSlide = 0;
            const totalSlides = slides.length;

            // Function to update which slide is visible
            function showSlide(slideIndex) {
                // Hide all slides first
                slides.forEach((slide) => {
                    slide.classList.remove('slide-active');
                });

                // Show the target slide
                slides[slideIndex].classList.add('slide-active');
                
                // Scroll the window to the top to make sure the new slide is visible
                window.scrollTo(0, 0);

                // Update the navigation controls
                updateControls();
            }

            // Function to update the state of the navigation buttons and counter
            function updateControls() {
                slideCounter.textContent = `${currentSlide + 1} / ${totalSlides}`;
                prevBtn.disabled = currentSlide === 0;
                nextBtn.disabled = currentSlide === totalSlides - 1;
            }

            // Event listener for the "Next" button
            nextBtn.addEventListener('click', () => {
                if (currentSlide < totalSlides - 1) {
                    currentSlide++;
                    showSlide(currentSlide);
                }
            });

            // Event listener for the "Previous" button
            prevBtn.addEventListener('click', () => {
                if (currentSlide > 0) {
                    currentSlide--;
                    showSlide(currentSlide);
                }
            });
            
            // Keyboard navigation
            document.addEventListener('keydown', (e) => {
                if (e.key === 'ArrowRight') {
                    // Trigger click only if the button is not disabled
                    if (!nextBtn.disabled) {
                        nextBtn.click();
                    }
                } else if (e.key === 'ArrowLeft') {
                    // Trigger click only if the button is not disabled
                    if (!prevBtn.disabled) {
                        prevBtn.click();
                    }
                }
            });

            // Set the initial state when the page loads
            showSlide(currentSlide);
        });
    </script>

</body>
</html>
